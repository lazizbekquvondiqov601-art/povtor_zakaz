# Railway service konfiguratsiyasi.
# Bot + Web bitta containerda (start_all.sh) — shared SQLite: /app/Data_Model.db

[build]
builder = "nixpacks"

[deploy]
startCommand = "bash start_all.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
